import React from "react";
import { Logo, SignupNav } from "../feature-components";

const Header = () => {
  return (
    <header>
      <Logo />
      <SignupNav />
    </header>
  );
};

export default Header;
